name: Video Downloader

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'Masukkan URL video'
        required: true
        type: string
      resolution:
        description: 'Pilih resolusi (1080p, 720p, 480p, 360p, 240p)'
        required: true
        default: '1080p'
        type: choice
        options:
          - '1080p'
          - '720p'
          - '480p'
          - '360p'
          - '240p'

jobs:
  download-video:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install -U yt-dlp

      - name: Run Video Downloader
        run: python url4.py -url "${{ github.event.inputs.video_url }}" -r "${{ github.event.inputs.resolution }}" -o video.mp4

      - name: Upload Video
        uses: actions/upload-artifact@v4
        with:
          name: downloaded-video
          path: "*.mp4"
